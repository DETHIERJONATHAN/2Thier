// Re-export des types Gmail depuis useGmailService pour compatibilité (isolatedModules)
export type { FormattedGmailMessage, GmailMessage, GmailThread, SendEmailRequest } from '../hooks/useGmailService';
