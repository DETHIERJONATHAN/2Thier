import ProfilePage from '../ProfilePage';

/**
 * Ce composant réutilise le composant ProfilePage existant pour la vue
 * des paramètres de profil dans la section settings.
 */
const ProfileSettings = () => {
  return <ProfilePage />;
};

export default ProfileSettings;
