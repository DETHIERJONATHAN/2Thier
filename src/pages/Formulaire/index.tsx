// Page principale du Form Builder (remplace SuperFormBuilderPage)
// Tous les composants, hooks, contextes, helpers, etc. liés au builder doivent être créés ici.
// Ce fichier sert de point d'entrée à la page principale du constructeur de formulaire.

import React from 'react';
import FormulaireLayout from './FormulaireLayout';

const FormulairePage: React.FC = () => {
  return <FormulaireLayout />;
};

export default FormulairePage;
