# Migration: add repeater_templateNodeLabels column

This migration adds the `repeater_templateNodeLabels` column (TEXT) to the `TreeBranchLeafNode` table to persist repeater template labels as JSON strings.
